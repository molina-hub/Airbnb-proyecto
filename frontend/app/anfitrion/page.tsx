import ApiWorkspace from "../../components/api-workspace";

/** Panel del anfitrión: propiedades, reservas recibidas e ingresos por período. */
export default function AnfitrionPage() {
  return <ApiWorkspace mode="mispropiedades" />;
}
